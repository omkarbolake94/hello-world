pipeline 
{

agent 
{
docker {image 'centos'}
}

stages

  {
  stage ('Test'){
  
             steps {
			 sh 'touch /home/root/build1.txt'
                         sh 'cat /etc/os-release >> /home/root/build1.txt'
			 sh ' df -h >> /home/root/build1.txt'
			 
			 }
			 
  
        }
  
  }

}
